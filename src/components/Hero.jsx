import HeroContent from './HeroContent';
import './Hero.css';

const Hero = () => {
    return (
        <div className="hero">
            <HeroContent />
        </div>
    );
};

export default Hero;